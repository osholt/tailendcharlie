<!--
  Preserved Android Auto declarations for a future, explicitly configured
  `androidAuto` build variant. The project deliberately defines no such Gradle
  flavour today, so this manifest is not merged into phone or Play releases.

  Keep the implementation and its tests healthy here, but do not move these
  declarations back to src/main until Android Auto distribution is approved.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="androidx.car.app.NAVIGATION_TEMPLATES" />
    <uses-permission android:name="androidx.car.app.ACCESS_SURFACE" />

    <application>
        <meta-data
            android:name="androidx.car.app.minCarApiLevel"
            android:value="1" />
        <meta-data
            android:name="com.google.android.gms.car.application"
            android:resource="@xml/automotive_app_desc" />
        <service
            android:name=".TailEndCharlieCarAppService"
            android:exported="true"
            android:label="Tail End Charlie"
            android:icon="@mipmap/ic_launcher">
            <intent-filter>
                <action android:name="androidx.car.app.CarAppService" />
                <category android:name="androidx.car.app.category.NAVIGATION" />
            </intent-filter>
        </service>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="androidx.car.app.action.NAVIGATE" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:scheme="geo" />
            </intent-filter>
        </activity>
    </application>
</manifest>
