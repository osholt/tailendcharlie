<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Android 11 and later hide other packages unless declared, and a
         text-to-speech engine is another package. Without this the engine is
         simply not found and spoken guidance fails silently, which is the worst
         outcome available: the switch is on, nothing is said, and nothing
         explains why (#286). -->
    <!-- Scanning a ride invitation, which is the only join path that works with
         no signal (#279). Requested at the point of scanning, and every other way
         in keeps working if it is refused. -->
    <uses-permission android:name="android.permission.CAMERA" />
    <queries>
        <intent>
            <action android:name="android.intent.action.TTS_SERVICE" />
        </intent>
    </queries>
    <uses-feature
        android:name="android.software.picture_in_picture"
        android:required="false" />
    <uses-feature android:name="android.hardware.bluetooth_le" android:required="false" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!--
      Nearby Connections still checks both Wi-Fi state permissions when
      advertising on Android 13+ (alongside NEARBY_WIFI_DEVICES). They are
      normal permissions, so keeping them uncapped adds no runtime prompt.
    -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <!--
      A ride keeps recording while the phone is in a pocket or showing another
      navigation app, which on Android means a foreground service typed for
      location (#205). geolocator_android already declares
      GeolocatorLocationService with android:foregroundServiceType="location";
      these are the permissions it needs to start it.

      Deliberately NOT ACCESS_BACKGROUND_LOCATION. A location-typed foreground
      service grants location while the app is backgrounded, so the extra
      permission - and the separate, alarming system prompt that comes with it -
      would buy nothing.
    -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADVERTISE" android:minSdkVersion="31" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" android:minSdkVersion="31" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" android:minSdkVersion="31" />
    <uses-permission android:name="android.permission.NEARBY_WIFI_DEVICES" android:minSdkVersion="32" />
    <uses-permission android:name="android.permission.ACCESS_LOCAL_NETWORK" android:minSdkVersion="37" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="androidx.car.app.NAVIGATION_TEMPLATES" />
    <!-- Required for a Car App Library navigation app to draw its map onto the
         Android Auto host surface. Play rejects the bundle without it. -->
    <uses-permission android:name="androidx.car.app.ACCESS_SURFACE" />

    <application
        android:label="Tail End Charlie"
        android:name=".RideRelayApplication"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher">
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="ride_updates" />
        <meta-data
            android:name="androidx.car.app.minCarApiLevel"
            android:value="1" />
        <!-- Android Auto discovers projected Car App Library apps through
             this descriptor. Without it the phone app installs normally, but
             the Android Auto host does not offer the app at all. -->
        <meta-data
            android:name="com.google.android.gms.car.application"
            android:resource="@xml/automotive_app_desc" />
        <service
            android:name=".TailEndCharlieCarAppService"
            android:exported="true"
            android:label="Tail End Charlie"
            android:icon="@mipmap/ic_launcher">
            <intent-filter>
                <action android:name="androidx.car.app.CarAppService" />
                <category android:name="androidx.car.app.category.NAVIGATION" />
            </intent-filter>
        </service>
        <service
            android:name=".RideRelayMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <meta-data
                android:name="flutter_deeplinking_enabled"
                android:value="false" />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data
                    android:scheme="https"
                    android:host="tailendcharlie.app"
                    android:path="/planner.html" />
            </intent-filter>
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data
                    android:scheme="https"
                    android:host="tailendcharlie.app"
                    android:path="/join.html" />
            </intent-filter>
            <!-- Opening a .gpx file (Files, a browser download, Mail) with a
                 correctly-set MIME type. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="content" android:mimeType="application/gpx+xml"/>
                <data android:scheme="file" android:mimeType="application/gpx+xml"/>
            </intent-filter>
            <!-- Same, but for sources that hand over a .gpx file without a
                 MIME type set (or a generic one) - match by extension instead. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <data android:scheme="content" android:host="*" android:pathPattern=".*\\.gpx"/>
                <data android:scheme="file" android:host="*" android:pathPattern=".*\\.gpx"/>
            </intent-filter>
            <!-- Appearing in another app's Share sheet for a .gpx file. -->
            <intent-filter>
                <action android:name="android.intent.action.SEND"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <data android:mimeType="application/gpx+xml"/>
            </intent-filter>
        </activity>
        <activity
            android:name=".GroupPipActivity"
            android:configChanges="screenSize|smallestScreenSize|screenLayout|orientation"
            android:excludeFromRecents="true"
            android:exported="false"
            android:resizeableActivity="true"
            android:supportsPictureInPicture="true"
            android:theme="@style/GroupPipTheme" />
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
