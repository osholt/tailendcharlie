# Tail End Charlie website

This is the static marketing site for `tailendcharlie.app`.

The page intentionally uses local, versioned project assets and no analytics,
web fonts, cookies, or client-side JavaScript. The production deployment is
configured in [`.github/workflows/deploy-website.yml`](../../.github/workflows/deploy-website.yml)
and requires the repository secrets documented there.
